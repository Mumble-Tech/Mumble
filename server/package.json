{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "prebuild": "eslint . --ext .ts && tsconfig.json --fix",
    "build": "tsc",
    "dev": "pnpm build && node .",
    "test": "pnpm build && node dist/socket.js && node dist/client.js"
  },
  "author": "deondreE",
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^5.44.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^8.28.0",
    "express": "^4.18.2",
    "typescript": "^4.9.3",
    "ws": "^8.11.0"
  },
  "devDependencies": {
    "@types/express": "4",
    "@types/node": "16",
    "@types/ws": "^8.5.3",
    "dotenv": "^16.0.3"
  }
}
