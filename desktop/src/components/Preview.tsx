
export const LivePreview =  () => {
    // What is the live preview --  Appplication
    // Video component that constatly is updated with the information on the screen
    // where does this video come from 

    return (
        <video></video>
    )
}